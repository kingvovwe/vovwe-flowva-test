# Flowva Rewards Dashboard Assessment

This is an identical replica of the Flowva Rewards Hub page, built for the frontend test.
It is a full stack web application with Supabase for authentication, database management, and state handling.

## Live Demo


## 🛠 Tech Stack
- **Frontend:** React (Vite), Tailwind CSS
- **Icons:** Lucide React
- **Animations:** CSS Transitions & Framer Motion logic
- **Backend:** Supabase (PostgreSQL, Auth, RLS Policies)
- **Routing:** React Router v6

## Key Features
- **Authentication:** Custom Login/Signup flow with Supabase Auth.
- **Rewards Hub:**
  - **Earn Tab:** View points balance, track daily streaks, and claim daily rewards (with 24h lockout logic).
  - **Redeem Tab:** Filter rewards by status (All, Unlocked, Locked, Coming Soon).

## Instructions to Setup

   git clone https://github.com/kingvovwe/vovwe-flowva-test.git

   cd flowva-rewards
   
   npm install

   Provide the Needed ENV Variables Found in the .env.example and change the name to .env or .env.local or any other .env file you prefer

   npm run dev

## The Database Schemas

create table public.profiles (
  id uuid references auth.users not null primary key,
  email text,
  full_name text,
  points integer default 0,
  streak_count integer default 0,
  referral_code text,
  created_at timestamp with time zone default timezone('utc'::text, now())
);

create table public.rewards (
  id bigint generated by default as identity primary key,
  title text not null,
  description text,
  points_required integer not null,
  category text, 
  is_active boolean default true,
  created_at timestamp with time zone default timezone('utc'::text, now())
);

create table public.daily_checkins (
  id bigint generated by default as identity primary key,
  user_id uuid references auth.users not null,
  check_in_date date default current_date
);

alter table public.profiles enable row level security;
alter table public.rewards enable row level security;
alter table public.daily_checkins enable row level security;



# Assumptions and Trade OFFS

1. I made the landing page and the initial dashboard simpler, with less contents
2. For the sidebar in the dashboards (home & rewards), i put the logout icon beside the profile
3. I inserted the rewards directly into the database
4. I added Google Sign & Signup Functionality
5. The (Earn More Points) section, i made the cards wider, so as not to compress the contents
6. I did my best in this project and hope it meets the viewers expectations, and thanks for giving me the opportunity